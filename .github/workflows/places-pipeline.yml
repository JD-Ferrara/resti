name: Google Places Pipeline

on:
  workflow_dispatch:
    inputs:
      area:
        description: 'Area(s) to fetch — single key, comma-separated, or "all"'
        required: true
        default: 'hudson_yards'
        type: choice
        options:
          - hudson_yards
          - chelsea
          - meatpacking
          - west_village
          - greenwich_village
          - hudson_square
          - soho
          - noho
          - tribeca
          - financial_district
          - little_italy
          - chinatown
          - nolita
          - lower_east_side
          - union_square
          - gramercy
          - flatiron
          - nomad
          - east_village
          - all

jobs:
  run-pipeline:
    name: Fetch → Filter → NTA Clip → Seed
    runs-on: ubuntu-latest
    env:
      FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: true

    steps:
      - name: Checkout repo
        uses: actions/checkout@v4

      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '22'
          cache: 'npm'

      - name: Install dependencies
        run: npm install

      - name: Write .env
        env:
          VITE_SUPABASE_URL: ${{ secrets.VITE_SUPABASE_URL }}
          GOOGLE_PLACES_API_KEY: ${{ secrets.GOOGLE_PLACES_API_KEY }}
          SUPABASE_SERVICE_ROLE_KEY: ${{ secrets.SUPABASE_SERVICE_ROLE_KEY }}
        run: |
          echo "VITE_SUPABASE_URL=$VITE_SUPABASE_URL" >> .env
          echo "GOOGLE_PLACES_API_KEY=$GOOGLE_PLACES_API_KEY" >> .env
          echo "SUPABASE_SERVICE_ROLE_KEY=$SUPABASE_SERVICE_ROLE_KEY" >> .env

      - name: Seed raw_places
        run: node scripts/seed-raw-places.js --area ${{ github.event.inputs.area }}

      - name: Run comparison (Hudson Yards only)
        if: github.event.inputs.area == 'hudson_yards'
        run: node scripts/compare-hudson-yards.js --save

      - name: Upload comparison report
        if: github.event.inputs.area == 'hudson_yards'
        uses: actions/upload-artifact@v4
        with:
          name: hudson-yards-compare-${{ github.run_number }}
          path: scripts/output/hudson-yards-compare.json
          retention-days: 30
